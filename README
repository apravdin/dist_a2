=========================
Compiling
=========================

> cd ROOT_DIR
> make

This will generate `./binder.out` and `src/librpc`

=========================
Cleaning
=========================

> cd ROOT_DIR
> make clean

=========================
Running
=========================

> ./binder.out

This will start the binder and display the `BINDER_ADDRESS` and `BINDER_PORT`.

> ./server.out

This will start up the example server provided with this assignement.

> ./client1.out

Same as server except the client.

